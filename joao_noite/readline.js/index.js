const  readlineSycn = require(`readline-sycn`)
const nome = readlineSycn.question(`qual o seu nome?`)
console.log(`ola ${nome}, tudo bem?`)

//

let i = 0 
let soma = 0
while (i < 5) {
const pergunta1 = Number(readlineSycn.question(`qual o seu nome`));
i++
}